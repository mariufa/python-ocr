# python-ocr

* sudo apt install python3-venv
* cd "project-root"
* python3 -m venv venv
* . ./venv/bin/activate
* pip install -r requirements.txt
* cd src
* python server.py

curl -F 'file=@myfile' http://localhost:5000/
